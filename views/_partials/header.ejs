<!DOCTYPE html>
<html>
    <head>
        <title>Where The Party At?</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.6/css/bootstrap.min.css" integrity="sha384-rwoIResjU2yc3z8GV/NPeZWAv56rSmLldC3R/AZzGRnGxQQKnKkoFVhFQhNUwEyJ" crossorigin="anonymous">
        <link rel="stylesheet" href="/stylesheets/main.css">
        <link href="https://fonts.googleapis.com/css?family=Cabin" rel="stylesheet">
        
    </head>
    <body class="background-photo">
        <nav class="navbar navbar-toggleable-md navbar-light bg-faded navbar-fixed-top navbar-inverse">
            <div class="container-fluid">
                <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                  <span class="navbar-toggler-icon"></span>
                </button>
                <!-- <a class="navbar-brand" href="#">Navbar</a> -->
                <div class="collapse navbar-collapse" id="navbarNav">
                  <ul class="navbar-nav">
                    <li class="nav-item active">
                      <% if (user) { %>
                        
                       <li class="nav-item"><a style="color: red" href="/logout">Logout</a></li>
                       <% } else { %>
                       <li class="nav-item"><a style="color: red" href="/auth/google">Log in with Google</a></li>
                       <% } %>
                    </ul>
                </div>
            </div>
            </nav>
    <!-- new html below -->
    
    <div class="container">
            <% if (user) { %>
       
            <ul style="color: white">Hello, <%=user.name %> </ul>
            <% } %>
            <ul class="left">
               <% if (user) { %>
       
               <!-- <h3>Hello <%= user.name %> </h3>
                   <h5>for dev: <%= user.googleId %> </h5> -->
               <% } %>
               </ul>
            <ul class="right">
       
              <% if (user) { %>
               
              <li class="btn btn-primary-outline"><a style="color: white" href="/logout">Logout</a></li>
              <% } else { %>
              <li class="btn btn-primary-outline"><a style="color: white" href="/auth/google">Log in with Google</a></li>
              <% } %>
       
           </ul><br>
    
    <div class="col-xs-12 text-center">
       <span><img src="https://i.imgur.com/xa83WZ8.png"></span>
    </div> 
    </div>

